// Arrow Functions / map

let arr = [1, 5, 8, 10, 20];

arr = arr.map(item => item * 2);

// melhorando a semantica para funções mais complexas e tonrnado a função reutilizável
//const multiplicaItemPorDois = item => item * 2;
//arr = arr.map(multiplicaItemPorDois);

